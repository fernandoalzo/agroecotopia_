export interface Product {
  slug: string;
  name: string;
  description: string;
  price: string;
  categoria: string;
  unidad: string;
  tag: string;
  emoji: string;
  photos: string[];
  stock: number;
}
